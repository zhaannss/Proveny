# Changelog

## Unreleased

- Added `POST /auth/register` to `openapi.yaml` to satisfy the assignment's mandatory baseline (registration is required even though the original blueprint OpenAPI only listed login/refresh/logout).
- Added `POST /analysis/one-shot` to implement the SylLab one-shot comparison requirement (not present in the original OpenAPI).

